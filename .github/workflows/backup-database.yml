name: database-backup-cron-job

on:
  push:
    branches: 
      - main

jobs: 
  hello: 
    runs-on: ubuntu-latest
    steps: 
      - name: Checkout Code
        uses: actions/checkout@v4
      - name: Print Hello
        run: echo "Hello"
